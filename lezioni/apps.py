from django.apps import AppConfig

class LezioniConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'lezioni'
    verbose_name = "Gestione Lezioni" # Nome leggibile per l'admin
